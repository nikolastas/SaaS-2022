# THIS MUST BE DELETED IN THE FINAL BUILD

If database configuration fails for no reason run this:

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root'

On SQL QUERY console
